import { RequestHandler } from "express";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const updateCourseFilesHandler: RequestHandler = (req, res, next) => {
  res.json({
    message: "not implemented",
  });
};
